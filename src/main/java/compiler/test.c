#include<stdio.h>

int main() {
    int a,b,c,d;
    a = 0, b = 2, c = 3, d = 0;
    int e = a+c;
    while(1)
        return a||b;
    if (!(a+d)) printf("Entre al if\n");
    //printf("%d, %d, %d, %d", a,b,c);
}
// Declaración var simple: TIPO ID;
// Declaración var compuesta: TIPO (ID, ID, ...);
// Asignación variable: ID = VALUE;
// Asignación compuesta: ID = VALUE, ID = VALUE,...;
// Declaración y asignación: TIPO (ID, Asignacion variable, Aisgnación variable,...);

// int a, b;
// a = 3, b = 4;
// int a;
/*




*/
